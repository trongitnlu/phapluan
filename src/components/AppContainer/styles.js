import {StyleSheet} from 'react-native';

import SIZE from 'constants/size';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    height: '100%',
    paddingHorizontal: SIZE.DEFAULT_PADDING_HORIZONTAL,
  },
});

export default styles;
